# agentbastion / bastiongate skill policy generated by bastionskill
# skill: demo-skill  risk=critical
default: allow
skills:
  demo-skill:
    verdict: deny
    reasons:
      - hook-install
      - network-egress
    block_capabilities:
      - hook
      - network
